.DS_Store
.idea/
.env

# Rust build output — workspace target directory lives at the repo root now.
/target/
src/**/target/

# Tantivy index + IMDb TSV cache written at runtime by the ingestor.
src/RustServer/data/

Cargo.lock.bak
